# {{ ansible_managed }}
#
# x11vnc server

description     "x11vnc server"

start on filesystem or runlevel [2345]
stop on runlevel [!2345]

respawn
respawn limit 10 5

exec /usr/bin/x11vnc -forever -display :0 -auth guess -scale 800x600
