# {{ ansible_managed }}
#
# Websockify VNC proxy server

description     "Websockify server"

start on filesystem or runlevel [2345]
stop on runlevel [!2345]

respawn
respawn limit 10 5

exec /usr/share/websockify/websockify.py {{ websockify_port }} localhost:5900
